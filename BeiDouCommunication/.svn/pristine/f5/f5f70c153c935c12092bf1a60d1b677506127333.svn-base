package com.zhbd.beidoucommunication.service;

import android.app.Service;
import android.content.Intent;
import android.os.IBinder;
import android.support.annotation.Nullable;
import android.util.Log;

import com.zhbd.beidoucommunication.http.NetWorkUtil;
import com.zhbd.beidoucommunication.utils.DataProcessingUtil;

import java.util.Arrays;

/**
 * Created by zhangyaru on 2017/8/21.
 */

public class GlobalService extends Service {

    @Override
    public IBinder onBind(Intent intent) {
        return null;
    }

    @Override
    public void onStart(Intent intent, int startId) {
        super.onStart(intent, startId);
        //Log.e("error", "GolbalService.onStart():service onStart");
        new Thread() {
            @Override
            public void run() {
//                Log.e("error","开始接受消息");
                // 开始发送心跳包
                //NetWorkUtil.startThreadSocket();
                // 先循环接收消息,再开始发消息,保证所有都能接收到
                while (true) {
                    // 接收收到的数据
                    byte[] receiver = NetWorkUtil.alwaysReceiver();
                    //Log.e("error", Arrays.toString(receiver));
//                    try {
//                        sleep(2000);
//                    } catch (InterruptedException e) {
//                        e.printStackTrace();
//                    }
                    // 解析数据
                    DataProcessingUtil.receiveDataPackage(receiver);

                }
            }
        }.start();
    }
}
